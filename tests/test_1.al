procedure test_1 is
	n: integer;
	m: integer;
    k: boolean;
    c: character;
    --v: ARRAY(4..1000) OF CHARACTER;
    --t: ARRAY(1..1000) OF boolean;
----------------------------------------------------------
begin
    --get(c,n,c);
    --put(n+24+36);
    --put(int2char(9));
    --mcd(n, m);
    put(3, c);
    --k := (k and true) or (true or false or k or (k and k));
end;
